# PixelOS Automated Install - OpenBSD 7.6
# Mode OFFLINE : les sets sont lus depuis le disque local (clé USB)
#

System hostname = agricol-server
Which network interface = em0
IPv4 address for em0 = dhcp
IPv6 address for em0 = none
Password for root = ************
Public ssh key for root = 
Allow root ssh login = yes
Confirm root ssh key = no
Which disk is the root disk = sd0
Use whole disk for GPT = yes
Use (W)hole disk or (E)dit = whole
Set uid on sd0 = yes
Location of sets = disk
Select sets = -game* -x* -xbase* -xfont* -xserv* -xshare*
Directory containing sets = /76
Which disk contains the install media = sd1
Location of sets = disk
Path to the SHA256.sig file = /76/SHA256.sig
Set name(s) = done
Continue without verification = yes
Use installed SSH configuration = yes
